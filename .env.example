# NanoGemClaw Configuration

# Telegram Bot Token (Required)
# Get from @BotFather
TELEGRAM_BOT_TOKEN=

# Gemini API Key (Required for Image Generation and STT fallback)
# Get from Google AI Studio
GEMINI_API_KEY=

# Speech-to-Text Provider (Optional)
# Options: 'gemini' (default, free, multimodal) or 'gcp' (paid, dedicated API)
STT_PROVIDER=gemini

# Google Cloud Credentials (Required if STT_PROVIDER=gcp)
# Path to service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=

# Webhook (Optional)
WEBHOOK_URL=
WEBHOOK_EVENTS=error,alert

# Gemini Model (Optional)
# Default: gemini-3-flash-preview (fixed, no auto-switching)
# Uncomment to override:
# GEMINI_MODEL=gemini-3-pro-preview

# Container Configuration (Optional)
CONTAINER_TIMEOUT=300000
